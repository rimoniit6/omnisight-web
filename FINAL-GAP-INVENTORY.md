# FINAL GAP INVENTORY — 95 → 100 Certification Pass (2026-08-17)

Every remaining risk from the 95/100 certification re-verified from source, with the chosen disposition.

| Risk | Current State (verified from source) | Severity | Disposition | Verification planned |
| --- | --- | --- | --- | --- |
| R1 In-memory rate limiter (single-instance only) | `src/lib/rate-limit.ts` is a per-process `Map`; 23 call sites; adequate today but not topology-independent | High (security-critical limits) | **ELIMINATE** — PostgreSQL token-bucket limiter (atomic upsert, cross-instance), same API shape, async, fail-closed for security-critical keys, expiry sweep | Concurrent race test, multi-client test, window expiry, fail-closed test, 10→429 live |
| R2 Live Monitor 5–15 s latency | `mini-services/live-updates` polls PostgreSQL every 5 s (durable cursor); agent reports in ~10 s windows | Medium (product SLA) | **ELIMINATE (poll latency)** — PostgreSQL `LISTEN/NOTIFY` wake-up: ingestion writes fire `pg_notify`, the service wakes the poller immediately (debounced); 5 s poll + durable cursor remain as catch-up/recovery. SLA: ingestion→delivery **P95 < 1 s**; end-to-end ≤ agent window + <1 s | Trigger migration test, wake-path unit test, live latency sample |
| R3 Agent-side tamper detection gap | Server has heartbeat freshness (`effectiveDeviceStatus`), one-active-device rule, revocation; no explicit missing/tamper detection | Medium | **ELIMINATE (server-authoritative)** — new `device_missing` anomaly type via the existing dedupe-keyed anomaly job: active approved device silent past threshold while monitoring consent is on; version-mismatch flagged at heartbeat. Client-side binary tamper detection is documented as not security theater (out of scope; server authoritative) | Anomaly job tests, version-mismatch test, no-false-positive-on-network-outage test |
| R4 Unused break/tamper/anomaly agent endpoints | Server endpoints exist + are heavily tested (break-hardening BH-*, agent-hardening P2-4, notification-alerting) and agent-token-secured; the **agent-side** `BreakApi`/`TamperApi`/`AnomalyApi` client classes in `desktop-agent/src/api/heartbeat.ts` are never instantiated | Low | **FORMALLY INTEGRATE** — keep server endpoints (tested, secured, org-scoped, rate-limited); delete the dead agent-side client classes; document each endpoint's purpose/status in API.md | grep for remaining references; tsc; test suite |
| R5 Dual enrollment paths | `POST /api/agent/register` (legacy, employeeId+bcrypt password) and zero-touch `DeviceClaim` both end in admin approval, one-active-device rule, audit, rate limits | Low | **ACCEPT (Option B)** — explicit dual-mode; both already share security guarantees; add a parity test proving identical device lifecycle + one-active-device conflict applies to both | Enrollment parity test |
| R6 Mobile matrix untested | Only desktop viewport smoke-tested | Low | **ELIMINATE** — Playwright matrix across 320/375/390/430/tablet/landscape on all major pages: overflow, clipping, dialog access, chart/table usability | Matrix script output |
| R7 5 skipped agent-build E2E tests | Gated on `RUN_AGENT_BUILD_E2E=1`; require a real Windows Electron build (electron-builder + MSVC native addon) | Low | **ATTEMPT** — prepare environment and run; if the MSVC/Windows-SDK toolchain is genuinely absent, document the exact dependency and provide a deterministic CI-compatible strategy (smoke the build pipeline with a stub) | Run or documented blocker |

**Gate definition (100/100):** P0/P1/P2 = 0 · no unresolved security/tenant/credential/integrity issue · rate limiting topology-safe · realtime SLA met · tamper detection meets documented capability · enrollment paths secure + intentional · mobile matrix verified · critical E2E executed or deterministically strategy-covered · TS 0 · lint 0 errors · tests 0 failures · 0 unjustified skips · build PASS · startup PASS · backup/restore PASS · live traces reconcile · docs match implementation · remaining risks eliminated or formally accepted as product requirements.
